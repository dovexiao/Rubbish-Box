# Figma 集成方案

## 方案对比

### 方案1: Figma Dev Mode（最简单，推荐）⭐
**优点：**
- 无需安装任何工具
- 直接在Figma中查看所有样式值
- 支持复制CSS/React Native代码
- 免费使用

**使用方法：**
1. 在Figma中打开设计文件
2. 点击右上角"Dev Mode"按钮
3. 选择任意元素，右侧面板显示：
   - 颜色值（HEX, RGB）
   - 字体大小、行高
   - 间距、边距
   - 圆角、阴影等
4. 可以直接复制代码片段

**适用场景：** 日常开发时快速查看样式值

---

### 方案2: VS Code 插件（在编辑器中查看）
**插件推荐：**
- **Figma** (官方插件)
- **Figma Tokens**

**安装：**
```bash
# 在VS Code中搜索并安装 "Figma" 插件
```

**优点：**
- 在代码编辑器中直接查看Figma设计
- 无需切换窗口
- 支持同步设计令牌

**缺点：**
- 需要VS Code
- 需要Figma账号授权

---

### 方案3: Figma 插件导出设计令牌（自动化）
**推荐插件：**
- **Figma Tokens** - 导出设计令牌为JSON
- **Design Tokens** - 导出颜色、字体等
- **Style Dictionary** - 转换为多平台代码

**使用方法：**
1. 在Figma中安装插件
2. 选择要导出的样式
3. 导出为JSON文件
4. 在项目中引入使用

**优点：**
- 自动化同步
- 支持版本控制
- 可以批量导出

---

### 方案4: 简单的命令行工具（轻量级）
**功能：**
- 通过Figma API获取设计文件信息
- 生成样式常量文件
- 支持按需更新

**优点：**
- 轻量级，不依赖IDE
- 可以集成到CI/CD
- 灵活可控

**缺点：**
- 需要Figma API Token
- 需要编写脚本

---

### 方案5: 浏览器书签工具（最轻量）
**功能：**
- 一键打开Figma设计文件
- 快速查看样式值
- 无需安装任何工具

**优点：**
- 零配置
- 跨平台
- 不占用项目空间

---

## 推荐方案组合

### 日常开发：方案1（Dev Mode）
- 快速查看样式值
- 复制代码片段

### 团队协作：方案3（插件导出）
- 统一设计令牌
- 版本控制
- 自动化同步

### 深度集成：方案4（API工具）
- 完全自动化
- 可定制化
- CI/CD集成

---

## 快速开始

### 使用 Dev Mode（推荐新手）
1. 打开Figma设计文件
2. 点击右上角"Dev Mode"
3. 选择元素查看样式
4. 复制需要的值到代码中

### 使用VS Code插件
1. 安装"Figma"插件
2. 登录Figma账号
3. 在编辑器中打开Figma文件链接
4. 查看设计并复制样式

### 使用插件导出令牌
1. 安装"Figma Tokens"插件
2. 选择要导出的样式
3. 导出为JSON
4. 在项目中引入JSON文件

